import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  primary: {
    color: '#000',
    fontSize: 22,
    fontWeight: 'bold',
  },
  secondary: {
    color: '#000',
    fontSize: 18,
    fontWeight: 'bold',
  },
});

export default styles;
